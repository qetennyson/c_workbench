#include <stdio.h>
#include <string.h>
#include <ctype.h>

// two functions can character map:
toupper();
tolower()

if (toupper(userInput) == 'Y') // just like Python
  { printf("yes\n"); }
else
  { printf("no\n"); }
